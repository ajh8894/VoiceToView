<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.swmem.voicetoview"
    android:versionCode="1"
    android:versionName="1.0" >

    <!-- Permissions used by this bundle -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="22" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.AssistantActivity"
            android:label="@string/title_activity_assistant" >
        </activity>
        <activity
            android:name=".HideActivity"
            android:label="@string/title_activity_hide" >
        </activity>

        <service
            android:name=".service.VoiceToViewService"
            android:process=":remote" >
            <intent-filter>
                <action android:name="com.swmem.voicetoview.service.VoiceToViewService" />
                <action android:name="com.swmem.voicetoview.service.IRemoteService" />
            </intent-filter>
        </service>

        <receiver android:name=".broadcastreceiver.PhoneStateReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
                <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
            </intent-filter>
        </receiver>
    </application>

</manifest>